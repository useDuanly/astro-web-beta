---
export interface Props {
  href: string;
  body: string;
  class?: string;
}

const { href, body, class: className } = Astro.props;
---

<a class={className} href={href}>{body}</a>

<style>
  a {
    color: #ffff;
  }
</style>
